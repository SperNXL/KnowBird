package com.knowbird.settings;

/**
 * 设置-item项类型
 *
 */
public enum SettingsType {
    // 用户信息项
    TYPE_USER_INFO,

    // 分组标题（通用、其他）
    TYPE_TITLE,

    // 开关项
    TYPE_SWITCH,

    // 点击项（如公告、成就）
    TYPE_CLICK,

    // 版本信息项
    TYPE_VERSION
}
